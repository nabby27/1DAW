Algoritmo: primo

Entero: x, num, resultado
Booleano: primo

INICIO
    x = 0
    primo = verdadero
    escribir("Dime un numero")
    leer num
    mientras (x < num) entonces
        si ((x != 1) y (x != num) y (num % x == 0)) entonces
            primo = falso
            finMientras
        finSi     
        x++
    finMientras
    si (primo = verdadero) entonces
        escribir("Es primo")
    finSi
FIN_ALGORITMO